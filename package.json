{
  "name": "n-studio-relay",
  "version": "1.0.0",
  "description": "Generic CORS relay for N Studio — forwards requests to allowlisted AI provider APIs so the browser can call them directly.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
